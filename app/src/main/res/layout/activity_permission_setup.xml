<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimary"
        app:layout_constraintTop_toTopOf="parent"
        app:title="Required Permissions"
        app:titleTextColor="@color/white" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/btnContinue"
        app:layout_constraintTop_toBottomOf="@id/toolbar">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Standard Permissions -->
            <include
                android:id="@+id/itemStandardPermissions"
                layout="@layout/layout_permission_item" />

            <!-- Overlay Permission -->
            <include
                android:id="@+id/itemOverlay"
                layout="@layout/layout_permission_item" />

            <!-- Accessibility Service -->
            <include
                android:id="@+id/itemAccessibility"
                layout="@layout/layout_permission_item" />

            <!-- Usage Stats -->
            <include
                android:id="@+id/itemUsageStats"
                layout="@layout/layout_permission_item" />

            <!-- Battery Optimization -->
            <include
                android:id="@+id/itemBattery"
                layout="@layout/layout_permission_item" />

            <!-- Device Admin -->
            <include
                android:id="@+id/itemDeviceAdmin"
                layout="@layout/layout_permission_item" />

        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/btnContinue"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:backgroundTint="@color/colorPrimary"
        android:text="Continue"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
